# Global instructions

Loaded by Claude Code in every project, including when it runs inside Zed as an
external (ACP) agent. Project `CLAUDE.md` files take precedence.

## How I work

- Read before you write. Prefer editing an existing file over creating a new one.
- Match the surrounding code: naming, comment density, error handling, test style.
- Small, reviewable changes. No refactors I did not ask for.
- Report outcomes faithfully — failing tests and skipped steps get said out loud.

## Guardrails

- Ask before anything destructive or outward-facing: force-push, migrations,
  deploys, deletes, publishing.
- Never commit secrets. Never read `.env` unless I ask.
- Don't add dependencies without saying why the standard library won't do.

## Setup

- Skills: `~/.claude/skills`, shared with Zed through `~/.agents/skills`.
- User-scope MCP servers live in `~/.claude.json` (`claudeset mcp add --scope user`).

<!-- Generated by claudeset on {{date}}. Edit freely; claudeset will not overwrite it. -->
