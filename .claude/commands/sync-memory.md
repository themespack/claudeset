---
description: Review recent changes and update project memory files.
---

Review what changed in this session and keep the project memory current:

1. Read the files in `.claude/memories/`.
2. If architecture changed, update `architecture.md`.
3. If new tools/deps were added, update `stack.md`.
4. Append finished work and new tasks to `todo.md`.

Only edit in place. Never overwrite a memory file wholesale.
